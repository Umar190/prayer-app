# Native review prompt

Use the current iOS StoreKit review API and Google Play In-App Review at an appropriate natural point after meaningful use. The system decides whether the actual prompt is shown. Do not ask for a positive rating in your own dialog.

The PWA uses a delayed, dismissible review card until the real store URL is configured.
